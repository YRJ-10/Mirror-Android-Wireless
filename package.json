{
  "name": "mirror-android-wireless",
  "version": "1.0.0",
  "description": "Smart Launcher Scrcpy untuk Windows",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Mirror Wireless\" --platform=win32 --arch=x64 --out=dist --overwrite --asar=false"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-packager": "^17.1.2"
  }
}
